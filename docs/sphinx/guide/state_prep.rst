State preparation
=================

Hartree-Fock references, the UCC family, Givens / Slater-determinant preparation, and operator pools; an input injected into the algorithm factories via `state_prep=`. *(Content migrated in phase 2.)*
